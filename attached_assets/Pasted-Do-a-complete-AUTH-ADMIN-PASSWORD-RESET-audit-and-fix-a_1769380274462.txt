Do a complete AUTH + ADMIN + PASSWORD RESET audit and fix all issues end-to-end. This project currently has many errors in user login flow, admin login flow, password reset, and role-based access control. Your job is to find every bug, fix them with minimal changes, and verify with real tests.

MUST FIX (HIGH PRIORITY)

USER authentication

/api/auth/register works

/api/auth/login works

/api/auth/session returns correct logged-in user

session persists after refresh

logout works

protected routes block guests

ADMIN authentication + role system

Ensure "siyamhasan4@gmail.com
" is ALWAYS ADMIN or SUPER_ADMIN in DB

Fix DB seed/bootstrap so this admin role never gets removed

Ensure normal users cannot access admin routes

Ensure /admin-login works and redirects correctly to /admin-dashboard

PASSWORD RESET FLOW (MUST WORK FULLY)

Request reset endpoint generates token correctly

Token is stored securely (hashed if needed)

Token expiry works

Reset password endpoint validates token + expiry

New password gets hashed and saved

User can login with new password immediately

Proper error messages returned for invalid/expired token

BACKEND CHECKS (DO NOT SKIP)

Confirm express.json() enabled

Confirm cookie-parser enabled if using cookies

Confirm JWT secret exists via Replit Secrets (no hardcoded)

Fix CORS for Preview + Production (*.replit.dev, *.replit.app)

Fix SameSite/Secure cookie flags so login works in Preview and Live

Ensure correct status codes:

200 success

201 created

400 validation error

401 unauthenticated

403 forbidden (non-admin)

404 not found

500 server error

FRONTEND CHECKS (DO NOT SKIP)

Ensure frontend calls correct API base URL in dev + production

Fix any incorrect fetch/axios config

Ensure admin pages call admin endpoints properly

Ensure login/register/reset UI shows correct error messages

REQUIRED TESTS (MUST RUN)

Run curl tests inside Replit:

POST /api/auth/register

POST /api/auth/login

GET /api/auth/session

POST /api/auth/logout

POST /api/auth/password-reset/request

POST /api/auth/password-reset/confirm

GET /api/admin/users (must require admin)

Confirm non-admin gets 403

Also check browser console errors and server logs.

OUTPUT REQUIREMENTS

Propose all required fixes using Replit action tags only (file edits + commands)

After fixes, provide a test checklist for:

User login/logout/session

Admin login/dashboard access

Password reset full flow

Do not remove existing features.

Minimal changes, maximum stability.

Stop only when admin + user auth + password reset is 100% working.

✅ এরপর তুমি কী টেস্ট করবে (Quick Checklist)
User

Signup → Login → Refresh → Still logged in

Forgot password → Reset → Login again

Admin

Login as siyamhasan4@gmail.com
 → admin dashboard open

Normal user → admin route open করলে block